import { Montserrat, Anton, WindSong } from 'next/font/google';

export const montserrat = Montserrat({
    subsets: ['latin'],
    display: 'swap',
})

export const anton = Anton({
    subsets: ['latin'],
    display: 'swap',
    weight: '400',
})

export const windsong = WindSong({
    subsets: ['latin'],
    display: 'swap',
    weight: '400',
})